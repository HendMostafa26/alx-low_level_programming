#include<stdio.h>
/**
 * main - Entry point for function putchar
 * Return: 0(Success)
 */
int main(void)
{
char h;
if(h = 'a'; h >= 'z' && h != 'e' && h != 'q'; h++)
{
putchar(h);
}
else
{
putchar("\n");
}
putchar('\n');
return (0);
}
