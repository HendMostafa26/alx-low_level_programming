#include<stdio.h>
/**
 * main - Entry point for function putchar()
 * Return: 0 (Success)
 */
int main(void)
{
char h;
char new_line=  ;
for (h='a'; h<='z' ; h++)
{
putchar(h);
putchar(new_line);
}
return (0);
}
