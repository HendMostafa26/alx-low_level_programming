#include"main.h"
/**
 * _islower - Function used to see if it is lower or not
 * Return: 0 is success and 1 is failure
 */
int _islower(int c)
{
if (c == 'c')
{
return (0);
}
else
{
return (1);
}
return (0);
}
