#include"main.h"
/**
 * rev_string - reverse string
 *@s: string
 * Return: void
 */
void rev_string(char *s)
{
  int i = 0;
  int j;
  while (*s != '\0')
    {
      _putchar(*s++);
      i++;
    }
  for (j = i; j > 0; j--)
    {
      _putchar(*s);
      s--;
    }
  _putchar('\n');
}
