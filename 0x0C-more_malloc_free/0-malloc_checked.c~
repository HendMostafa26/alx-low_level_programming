#include"main.h"
#include<stdlib.h>
/**
 * malloc_checked - Function to allocate memory
 * @b: Unsigned integer
 * Return: pointer
 */
void *malloc_checked(unsigned int b)
{
void *value = malloc(b);
if (value == 0)
{
exit(98);
}
return (value);
}
