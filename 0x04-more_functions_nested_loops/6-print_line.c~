#include"main.h"
/**
 * print_line - print straight line
 * @n: Integer
 * Return: void
 */
void print_line(int n)
{
  if (n > 0)
    {
      _putchar(95);
    }
  else
    {
      _putchar('\n');
    }
}
