#include"main.h"
/**
 * _strlen_recursion - return length of string
 * @s: string
 * Return: integer
 */
int _strlen_recursion(char *s)
{
  int i;
  int j = 0;
  for (i = 0; s[i] != '\0'; i++)
    {
      j++;
    }
  return (i);
  }
